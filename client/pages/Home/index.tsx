// ==========================================================================
// PAGES / HOME / #INDEX
// ==========================================================================

/**
 * Imports
 * 1. Import react
 *
 */
import React from 'react'; // [1]

/**
 * Home component
 *
 * @returns {React.ReactElement} React element
 * @example
 * <PageHome />
 *
 *
 */
export const PageHome = (): React.ReactElement => {
    return <p>Home</p>;
};
